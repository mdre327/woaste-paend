# woaste-paend

A Next.js App Router storefront concept for selling kullad chai. The home page uses a centered rolling clay cup that reacts to vertical swipe, wheel, and keyboard input.

## Scripts

- `npm install`
- `npm run dev`
- `npm run lint`
- `npm run build`
